using Counter.Configuration;
using Microsoft.Extensions.Configuration;
using System;
using System.IO;

namespace Test
{
	class Program
	{
		static void Main(string[] args)
		{
			//var margin = new Configuration.Thickness(0.5);

			//var config = new AppConfiguration {
			//	AppSettings = new AppSettings { Counter = 5},
			//	UISettings = new UISettings { TopMost = true, WindowHeight = 100, WindowWidth = 200},
			//	PlusButtonSettings = new ButtonSettings { Margin = margin, Opacity = 1},
			//	MinusButtonSettings = new ButtonSettings { Margin = margin, Opacity = 1 }
			//};

			//config.Dispose();

			//IConfiguration builder = new ConfigurationBuilder()
			// .SetBasePath(Directory.GetCurrentDirectory())
			// .AddJsonFile("appConfig.json", optional: false, reloadOnChange: true)
			// .Build();

		}
	}
}
